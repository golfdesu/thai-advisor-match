# WikiSkill Evolution & Patch Log
<!-- Reference: WikiSkill - Evolution Log (arXiv:2608.27454v1) -->

## [2026-09-01] Initial Wiki Initialization
- **Action:** Compiled foundational knowledge structure for Thai Universities (CMU ME, CPE, EE).
- **Traces Analyzed:** `cmu_robotics_mechatronics_batch` execution traces.
- **Skill Gating Status:** Baseline skills updated to reflect `SKILL.state` runtime and Wiki references.

### [2026-09-06 13:29:41] Wiki Knowledge Compilation
- **Traces Processed:** 34
- **Universities Updated:** leading_thai_universities, thammasat_university, mahidol_university, prince_of_songkla_university, khon_kaen_university, chiang_mai_university, chulalongkorn_university
- **New Verified Endpoints:** 28
- **Dead Links Flagged:** 3

### [2026-09-09] KKU Faculty of Nursing Full Acquisition (Wave 1)
- **Pipeline:** SKILL.state `cli_runner.py` (9 seed URLs, 71 profiles) → `faculty_massive_ingestion_runner.py` Batch 69 → 72 records embedded (768-dim) in local `advisor_match`.
- **Verified Endpoints:** nu.kku.ac.th executives, dept heads, program-coordinator (0 yield — flagged), 7 department pages.
- **Coverage Fix:** KKU Nursing 1 → 72 records; all embedded.

### [2026-09-09] TU Faculty of Engineering Acquisition (Wave 1)
- **Pipeline:** SKILL.state `cli_runner.py` (5 dept seed URLs → 82 profiles) → Batch 70 → 94 records embedded in local `advisor_match` (incl. 13 pre-existing deduped).
- **Verified Endpoints:** ece.engr.tu.ac.th/lecturer, ce/structural, me/professor_rangsit, che/professor; cic /person/ (0 yield — flagged).
- **Blocked Flagged:** sci.tu.ac.th (Incapsula), med.tu.ac.th (PDF-only rosters) — need SPA/PDF strategies per `data-scrape-spa` skill.
- **Coverage Fix:** TU Engineering 13 → 94 records.

### [2026-09-09] PSU Faculty of Science Acquisition (Wave 1)
- **Pipeline:** SKILL.state `cli_runner.py --no-wiki` (4 `personnel-lists` seed URLs → 224 clean profiles, all PSU emails) → Batch 71 → 228 records embedded in local `advisor_match`; 5 transient 503 embedding failures repaired via `embed_missing.py` (missing embeddings now 0).
- **Incident (fixed):** first run auto-seeded cross-faculty wiki URLs (FMS/Law) via `lookup_university_endpoints`, producing contaminated export — deleted before ingestion; restructured PSU wiki into Faculty-Scoped section + added `--no-wiki` flag to `cli_runner.py`.
- **Blocked Flagged:** Medicine (DNS dead; hospital.psu.ac.th → Google Sites org chart), Engineering (SPA, ce.eng JS-redirect only).
- **Coverage Fix:** PSU Science 5 → 228 records.

### [2026-09-09] Canonical Label Hygiene + Wave 2 Acquisition
- **Canonical merge (`enrichment/canonical_faculty_merge.py`):** 179 merged-university strings resolved via email/id-prefix evidence; variant labels unified ((TBS)/(KKBS)/(CBS)/CAMT/SIIT/CMMU/JGSEE/SGtech/สำนักวิชาววิทยาศาสตร์ typo/SUT program suffixes); composite labels split by department evidence (CU+MU allied/medtech → CU สหเวชศาสตร์ 27 + MU เทคนิคการแพทย์ 10; KU agro/vet split by id prefix; Siriraj+Rama label retained — rows carry only dept evidence, no university split possible without fabrication; Silpakorn arts split by dept).
- **Source-dataset sync (`canonical_datasets_fix.py`):** 38 dataset files canonicalized so ingestion re-runs don't regress; 317 changed rows re-embedded; SIT/FIBO variants patched in 5 more files.
- **Wave 2 acquisitions:** CMU Public Health `ph.cmu.ac.th/lecturer.php` → 12 records (was 3 incl. variants); MU Nursing `ns.mahidol.ac.th/nurse_th/administrator.html` → 19 (was 3); KKU Science head-dept + env + bio portals → 54 (was 12).
- **Blocked:** KMUTT FIBO staff page & KMITL Medicine staff (JS-rendered, 0 raw HTML), MU Nursing dept pages (intranet), vet.cmu.ac.th / econ.cmu.ac.th / sci.kku.ac.th DNS dead.
- **Final DB state:** 4,347 faculties, 0 merged-university strings, 0 duplicate-variant labels (2 legitimate composite labels remain, evidence-based), 0 missing embeddings.

### [2026-09-10] Sparse-Famous-Faculty Wave 1 (CU CommArts/Arts, MU PT/Music)
- **Trigger:** coverage query flagged famous faculties with ≤5 advisors (66 rows across 7 flagship universities).
- **Tooling added:** `scripts/crawlers/discover_sparse_faculty_seeds.py` (SERPAPI seed discovery, results checkpointed to `data/agent_states/seed_discovery_results.json`), `scripts/crawlers/spa_skill_state_runner.py` (static fetch first, Selenium `app/scrapers/browser_scraper.py` fallback when academic density < 5 — implements `data-scrape-spa` skill contract), `scripts/crawlers/clean_sparse_wave1.py` (deterministic mojibake/transliteration cleaner: strips Cyrillic/Arabic contamination from EN→TH transliterations, EN-fallback names, dedup by email/name).
- **Acquisitions:** CU CommArts `units-personnel` + `department-jr` → 26 (was 2); CU Arts `arts.chula.ac.th/th/team` → 26 (was 21, fills roster; domain = Faculty of ARTS, NOT Fine & Applied Arts — relabeled); MU Physical Therapy `staff_lecturer` → 74 (was 3); MU College of Music `/people/` + `/yamp/faculty-list/` → 203 (was 3; EN-name pages, 5 kept EN-fallback).
- **Blocked flagged:** CU FAA `faa.chula.ac.th` (Incapsula, Selenium bypass failed), CU Vet lecturer roster (XHR-only), CMU Vet vmcmu personnel tabs (XHR-only), CMU Pharmacy `menu/208` (admin-only), TU Pharmacy/Liberal Arts (DNS dead for la/pharmacy subdomains — pharmacy lives at `pharm.tu.ac.th`).
- **Ingestion:** `faculty_massive_ingestion_runner.py` Batches 84–87 → +329 records, 329/329 embedded 768-dim (22 empty-text rebuilds repaired post-run). Final DB: 5,061 faculties, 0 missing embeddings. Semantic spot-check (pgvector HNSW): CommArts/Music queries return expected new faculty in top-5.

### [2026-09-10] Sparse-Famous-Faculty Wave 2 (XHR API breakthroughs: CU Vet, CMU Vet, TU Pharmacy)
- **Tooling added:** `app/scrapers/network_capture.py` (Selenium CDP `Network.responseReceived` + `Network.getResponseBody` capture; optional click-through of tabs) — the breakthrough tool: reveals hidden XHR endpoints and static fallbacks invisible to raw-HTML probing.
- **CMU Vet (3 → 83):** discovered `vmcmu.vet.cmu.ac.th/pages/person/api/fetchDataPerson_api.php?typeData[type]=vet_subject-{1,2}` returning structured JSON (name TH/EN, email, research, branch, scopus/scholar/orcid). Full category switch map extracted from `/pages/person/js/person.js`. Pipeline: `scripts/crawlers/cmu_vet_api_pipeline.py` (API → RawFacultyProfile → FacultyStateReducer) → 82 verified.
- **CU Vet (4 → 178):** new-site XHR dead-ended, but **legacy site `www.vet.chula.ac.th/department/<slug>`** renders full rosters statically — 11 dept slugs crawled → 174 profiles with @chula.ac.th emails (Facebook/500-slug auto-discovered URLs correctly failed).
- **TU Pharmacy (1 → 43):** homepage nav reveals `/academicstaff` (42 profiles, emails partially cross-domain: some ภญ. keep mahidol/chula addresses — legit joint appointments).
- **Bug fix (wave1 files):** contamination regex had stripped `@` from emails (sayamon.schula.ac.th); regex extended with `:@`, all 6 sparse datasets regenerated with corrected emails, re-ingested (wave-1 rows updated in-place; no new inserts).
- **Ingestion:** Batches 88–90 → +298 records, 298/298 embedded; 3 residual missing-embedding rows repaired post-run. Final DB: **5,359 faculties, 0 missing embeddings**. Semantic spot-checks: vet-orthopedic query → CMU/CU/KU vets top-5; clinical-pharmacy query → TU Pharmacy top-5.

### [2026-09-10] Sparse-Famous-Faculty Wave 3 (7 faculties across 5 universities, +228)
- **Method:** batch DNS/academic-density probing of 55 candidate subdomains (2 rounds) → SERPAPI key rotation (key[0] exhausted, key[1] live) for exact faculty names → WP REST API enumeration (`wp-json/wp/v2/pages`) to reveal hidden staff pages (KU Sports Sci `?page_id=521/359` → `/lecturer/`, `/staff/`).
- **Acquisitions:** CMU Agriculture `agro.cmu.ac.th/mis2/personnel/personal_new.php` → 80 (was 4); KKU Architecture `arch.kku.ac.th/org-staff-academic` → 59 (was 2); CU Institute of Asian Studies `ias.chula.ac.th/personnel/` → 27 (was 1); KU Sports Sci & Health `sportsscience.kps.ku.ac.th/lecturer/` → 24 (was 1); MU Institute of Nutrition `inmu.mahidol.ac.th/th/advisors/` + `/executive/` → 23 (was 3); KU Veterinary Technology `vettech.ku.ac.th/vettech` + `/vetnurse` → 9 (was 2); CMU Fine Arts → 6 (was 5, partial roster behind Elementor sub-pages).
- **Blocked confirmed:** KU Architecture (roster behind `ku-work.ku.ac.th` login), MU Vet (no roster in DOM/XHR), CU FAA (Incapsula persists), ~20 DNS-dead subdomains logged in `leading_thai_universities.md`.
- **Ingestion:** Batches 91–97 → +228 records, 228/228 embedded; 23 residual rows repaired post-run. Final DB: **5,587 faculties, 0 missing embeddings**. Spot-checks: KKU landscape-arch → KKU Arch top-4; nutrition → INMU top-4; Asian-studies → IAS top-4; agriculture → CMU Agri top-4 (0.80+ similarity).

## 2026-09-10 — Elite Researcher & Field-Coverage Audit (Task 05 created)
- **Trigger:** ผู้ใช้ต้องการหา "อาจารย์เก่ง ผลงานวิจัยดี" + รู้ว่าสาขาไหนอาจารย์ยังน้อย
- **Method:** SQL audit + 57-field regex taxonomy (สร้าง `backend/scripts/audits/field_taxonomy.py`, `field_coverage_gap_analysis.py`, `elite_researcher_gap.py`) + MHESI national demand data (40,029 นศ.ใหม่ ป.โท ปีการศึกษา 2568)
- **Key findings:**
  - เฉพาะ 1,912/5,587 (34%) มี h-index; ~3,675 คนไม่มีข้อมูลวิจัยเลย (bias เข้าข้าง STEM)
  - สาขา demand สูงแต่ไม่มีตัวท็อป (h≥20 = 0): ศึกษาศาสตร์+ครุศาสตร์ (demand #1 ~7,254/ปี), รป.ม. (~2,830), นิติศาสตร์ (282 คน h สูงสุดแค่ 6), ท่องเที่ยว, การตลาด, HCI/UX, EdTech, ไซเบอร์, สถาปัตย์, จิตวิทยา
  - Data quality: ยศซ้ำใน full_name_th 5,409 แถว (97%), เจอ dup ข้ามมหาลัย (บิน จ้าว h=87 ทั้ง มธ./จุฬาฯ)
  - `_fetch_distinguished_advisors` (routes_universities.py) ยังไม่จัดอันดับด้วย h_index/citations — quick win รอแก้
- **Artifact:** `future_tasks/05_find_expert_researchers.md` + อัปเดต `future_tasks/README.md` + `DATA_SOURCES.md` (MHESI source pattern)
- **Next:** ทำตามขั้นที่ 1–4 ใน 05_find_expert_researchers.md (quick wins ก่อน แล้ว enrichment 3,675 คน ผ่าน OpenAlex/ThaiJO)

## 2026-09-10 (ผ่าเย็น) — Task 05 Quick Wins EXECUTED ✅
- **Fix 1:** `_fetch_distinguished_advisors` (routes_universities.py) — ORDER BY h_index DESC NULLS LAST, citations, publications (limit 25→40). Verified: chula signature now shows Doyle h=121 → ชญานิษฐ์ h=108 → เจนนิ h=102.
- **Fix 2:** ยศซ้ำ 5,409 แถว → strip ที่ Pydantic DTO layer (`schema.py`: `_TITLE_CANON_PATTERNS` + `_clean_display_name` + `model_validator` on FacultyMember & FacultyCardSchema). Sample-500: raw dup 433 → post-clean 0. Handles stacked prefixes (ศ.ดร.ศ.ดร.), long-form↔abbrev equivalence (รองศาสตราจารย์ ดร. ↔ รศ.ดร.), no-title dedup (ดร.ดร. → ดร.).
- **Fix 3:** Dedup by openalex_id — `scripts/audits/merge_duplicate_faculties.py` (dry-run/--apply, same-uni merge only, cross-uni kept as dual affiliation, list-field UNION + scalar first-non-null + embedding inheritance + lab ref re-pointing). Result: 5,587 → 5,497 (−90), orphan embeddings 0, remaining dup groups = 12 cross-uni only.
- **Fix 4 (บั๊กแฝง):** `/search/cold-email` always crashed — `req.degree_level`/`req.thesis_topic` don't exist on ColdEmailRequest → renamed to `intended_degree`/`research_topic`. Endpoint verified 200 with real generation. Pre-existing test failure resolved (11 passed).
- **Fix 5:** 5 orphan labs (lead_advisor_id → non-existent ids ku_agro_001 etc.) re-pointed to best-matched faculty by h-index (cassava→สโรจน์ h=48; smart-agri→พีระศักดิ์ h=39; MFU cosmetic→มยุรี h=27; MFU fungal→อรวรรณ h=32; SUT quantum→สุขิต h=42). Orphan count = 0.
- **Test status:** 11 passed; 1 pre-existing failure (`test_agentic_pipeline` expects legacy id scheme `cmu_eng_ee_001` vs reducer's `chiangmaiu_...`) — flagged in Task 05 doc.
- **Next:** ขั้นที่ 2 enrichment 3,600 คน (OpenAlex/ThaiJO) → ขั้นที่ 3 acquisition สาขาขาด → ขั้นที่ 4 UI badge

## 2026-09-10 (ค่ำ) — Task 05 ขั้นที่ 2–3 EXECUTED: OpenAlex wave1 + ThaiJO OJS3 fix + 4 acquisition batches ✅
- **OpenAlex (`enrich_openalex_author_metrics.py`, ใหม่):** wave 1 --apply → **+227 h-index** (1,823→2,050, 37.3%); homonym gate precision 100% vs ground-truth; 🐛 501 แถวถูก stamp `not_indexed` ผิดduring key-pool outage → clear กลับ NULL + เพิ่ม **canary health-gate** (known-good probe ก่อน stamp sentinel; API ตาย → abort clean 0 writes). Wave 2 (1,964 targets) โดน quota ทั้ง 3 keys + polite pool throttle → canary abort ถูกต้อง 0 writes → ข้ามตามคำสั่ง user, คิว daily reset.
- **ThaiJO rewrite:** `enrich_thaijo_publications.py` เดิมพัง (หน้า search เก่า) → เขียนใหม่เป็น **OJS3 aggregate shards so01–so06** + precision-gated `authors=` search; waves 1–4 → **656 แถวมีผลงาน ThaiJO credit** (wave 3: 3,753 targets +352; wave 4 หลัง Batch 101: 3,617 targets +274 ครอบคลุม TBS — TBS cohort มี pubs แล้ว 48 คน)
- **Acquisition 4 คณะ (SKILL.state ครบวงจร, Batches 98–101):**
  - 98 `swu_edu_api_pipeline.py` — edu.swu.ac.th WP `staff` API → **107** (กรองเจ้าหน้าที่ด้วย courtesy prefix นาย/นางสาว)
  - 99 `ssru_edu_api_pipeline.py` — edu.ssru.ac.th TH/EN accordion + DLP email pages → **53** (email/photo/วุฒิ/DLP 100%; 🐜 EN cards เขียน honorific.gluied "Assoc.Prof.Dr." → strip loop; 🐛 วุฒิ truncated เพราะ `\n` ใน source = soft wrap → ใช้ `<br>` เป็น delimiter เดียว)
  - 100 `tu_law_api_pipeline.py` — law.tu.ac.th WP `teacher` API (102 posts) → **32 verified, curated pubs 188 รายการ dict-shape+DOI** (ครั้งแรกที่นิติศาสตร์ไทยมีรายชื่อผลงานจริงใน DB; 🐛 `content.rendered` มี `(function($){...})(jQuery);` widget JS โผล่เป็น text ชื่อบุคคลอื่น → strip เฉพาะ blob + EN name จาก post slug; 🐛 `json.dumps` null → invalid Python → export ทุก pipeline ใช้ `pprint.pformat`)
  - 101 `tbs_staff_api_pipeline.py` — tbs.tu.ac.th `staff-sitemap.xml` 280→195 คน (collapse `_th/_en/_director/_secretary`) → **102 academic** (email 78, วุฒิ 90, photo 570×570; 🐛 PHOTO_REไม่ anchored โดน site logo ทุกแถว → bind ที่ `</div><div class="staff_generated_info"`)
- **Dedup gap ปิด:** reducer email pre-check มองไม่เห็น legacy rows ไม่มี email → ซ้ำข้าม id-family → **`merge_duplicate_faculties.py --by-name`** (โหมดใหม่: ตัดยศ normalize ชื่อ, merge ใน uni+faculty เดียว, richest-row-wins, list UNION, re-embed ผ่าน build_faculty_embedding_text) — รอบแรก 98 กลุ่ม (5,776→5,677), รอบ Batch 101 185 กลุ่ม (5,872→**5,685**)
- **ผลต่อ priority gap:** การตลาด/บริหาร **ปิดแล้ว** (TBS 95→149, h≥20 = 0→4, max h=37 — สาขาแรกในตารางที่ตัวท็อป h≥20 โผล่จาก acquisition); ศึกษาฯ+ครุ 206→340 (pubs 205); นิติฯ 282→328 (pubs 182); Cyber/HCI/PolSci/Tourism = ข้ามอย่างมีหลักฐาน (ConnectError ทุกแหล่ง, probe ซ้ำค่ำ 10 ก.ย.)
- **Final DB:** **5,685 faculties, 0 missing embeddings, h>0 = 2,030 (35.7%), pubs = 2,413 (42.4%), emails = 3,903 (68.7%)**

## 2026-09-10 (กลางคืน) — System Health + Bottleneck Hunt EXECUTED ✅ (3 audit agents: Postgres / backend API / frontend+E2E)
- **pgvector HNSW poisoning (ตัวใหญ่สุด):** `SET hnsw.ef_search=400` (workaround เก่าสำหรับ filtered ANN คืน 0 แถวบนเซตเล็ก) บังคับให้ planner **ทิ้ง** `ix_faculties_embedding_hnsw` → Seq Scan+Sort 15–39ms ทุก unfiltered semantic search; pgvector คิด cost เป็น tuples×ef → threshold ที่ plan flip = **ef≤40 on 5.7k rows (วัด 40/64/80)** → เปลี่ยนเป็น `ef=40 + hnsw.iterative_scan=strict_order` (vector 0.8.x แก้ zero-row ตัวจริง: ขยาย candidate ต่อจน filter ครบ LIMIT โดยระยะทางเรียงถูกต้อง; recall restore 5/5) ใน `core/database.py` `_HNSW_TUNE` (connect+checkout listeners) + **ตัด `SET LOCAL` ใน get_db ทิ้ง** (no-op จริงตาม expire semantics แต่มันมันเผาผลาญ 2 round-trip/request) — ผล: Index Scan 2.4ms, `idx_scan` increment ยืนยันจาก app จริง, courses/labs path ใช้ index ครบ
- **Cold-email 13–36s → 3.4–4.6s:** `embedding_service.generate_cold_email_ai` เรียก 3.6-flash แบบ unbounded thinking (วัด thought_tokens 949 แม้ตั้ง budget=100 — เป็น soft hint); A/B จริง: no-caps 17.9s / budget=100 8.5s / **3.5-flash-lite 2.7s** → สลับ chain เป็น lite-first + max_output_tokens=1500 (mirrors career-quiz), **ตัด gemini-2.5-flash ตาย (404) ที่เผา 3 retries/miss**, ใส่ `HttpOptions(timeout=60000)` ใน `genai.Client` (เดิม hung call block anyio worker นิรันดร์) — cached path 28ms
- **/search N+1:** `db_to_pydantic` อ่าน `embedding_text` ที่ query defer ไว้ → 1 SELECT/แถว (20 แถว = 21 SELECTs) + ส่ง text ~1KB/แถวออก network ทั้งที่ frontend ไม่เคยใช้ (grep 0) → ตัดออกจาก converter; instrument ยืนยัน **21→1 SELECT**, payload embedding_text with content = 0
- **Missing indexes (live + ซิงก์ docker/init.sql):** GIN trgm ×3 (university_th, university, faculty_th — ILIKE '%..%' filters บน hot path เดิม seq-scan), HNSW บน research_labs.embedding + semantic_cache.embedding (L2 lookup เดิม full-sort โตเรื่อย ๆ), btree `ix_faculties_h_index DESC NULLS LAST` (distinguished-advisor ORDER) + `autovacuum_vacuum_scale_factor=0.05/analyze 0.02` on faculties + ANALYZE (stats เน่าจาก ingestion คือ root cause จริงของ seq-scan ที่เคยเข้าใจผิดว่า `.filter(embedding IS NOT NULL)`)
- **signature-programs cold 653→416ms:** 32 ILIKE COUNT round-trips → `GROUP BY (university_th, university)` ครั้งละตาราง + substring match ใน Python (pairs ไม่กี่ร้อย) — **parity 16/16 เทียบ SQL ground-truth**, warm 17ms (X-Cache HIT)
- **semantic_cache.set dedupe:** variant ใหม่ไม่ append row ซ้ำอีก (ข้าม insert ถ้ามี entry ในรัศมี ≤0.10 เดิม); 🐛 ที่แก้ระหว่างทาง: อ่าน attribute หลัง `db.commit()` ไม่ได้ (expire_on_commit re-SELECT) → capture label ก่อน commit
- **Frontend:** Mounted Pattern ฟื้นฟูครบ 3 จุด hydration mismatch (`app/page.tsx` savedCourses/Advisors, `advisor/[id]`, `labs/[id]` — `useState(() => hasSavedId(...))` อ่าน localStorage ตอน hydrate → React 19 mismatch ปุ่ม ❤; SSR=false แล้ว apply ใน post-mount effect) + stale-render race guards: sequence-ref ใน `executeSearch` (6 branches, only-latest-wins) และ `cancelled` flag ใน 2 detail effects; `tsc --noEmit` ผ่าน, dev server 200 ทุก route (markers ที่ grep เจอ = `suppressHydrationWarning` prop ชอบธรรมไม่ใช่ error)
- **คงเหลือ (บันทึกรับทราบ, ยังไม่แก้):** (a) Thai search นอกพจนานุกรม 94-key `THAI_EN_SYNONYMS` → 53–80 vs English 92+ — เป็น coverage/quality gap ต้อง enrich faculty `*_th` embeddings แยกงาน; (b) clamp 99.0 makes top band flat; (c) career-quiz 4.8s ใช้ lite-first อยู่แล้วแต่ยัง sync-def; (d) all LLM endpoints sync `def` → threadpool-40 saturation risk (timeout 60s เป็นเพดานบรรเทาแล้ว); (e) compose.yaml shared_buffers=128MB บน RAM 7.5GB — ต้อง restart container (เลื่อน: service ค้าง), session-level `random_page_cost` ไม่จำเป็นหลัง ANALYZE+stats ถูก; (f) image URL rot ต้นทางจริง (rgstatic 403 hotlink, human.cmu 404) — avatar fallback wired แล้ว
- **Regression gate:** pytest 11/12 ผ่าน (ตัวเดียวที่ fail = `test_state_reducer_dedup_and_enrichment` พังมาก่อนหน้า documented ใน Task 05 ขั้นที่ 1), search battery live: semantic 619ms cold / 22ms repeat, courses/lab ~0.6–0.8s (embedding-call-bound), lists 11–37ms

## 2026-09-10 (ดึก) — DSA/Algorithm/Cybersecurity Research → APPLY EXECUTED ✅ (goal: เรียนรู้จาก Google/อื่น ๆ แล้วปรับใช้; 3 research agents: hybrid-search / cyber-audit / perf-DSA)
- **ถาวร gate ใหม่:** `scripts/audits/search_quality_benchmark.py` — 10 คู่ query TH/EN (off-dictionary ล้วน: ปรสิตวิทยา, ปฐพี, พหุปัญญา, กฎหมายรัฐธรรมนูญ…) วัด best/top1/recall + **TH→EN gap** + floors (`th best≥40/recall≥10%`, `en best≥85/recall≥20%`), exit≠0 เมื่อ breach — รันทุก wave
- **Baseline ที่วัดได้:** retrieval ถูกต้องแล้ว (top1 10/10 ทั้งสองภาษา) — ปัญหาจริงคือ **score calibration พังที่ clamp 0.99** (top band แบนราบ) + **TH→EN gap +23.3** (embedding EN-skew เพราะ expand_query ผูกฝั่ง query ฝั่งเดียว)
- **Wave 1 (calibration + lexicon):** sort บน unclamped composite (clamp เฉพาะ display — RRF lesson: cap AFTER ranking) + `THAI_EN_SYNONYMS` 94→139 keys (โบราณคดี/นิติ/ครุ/ปฐมวัย/ปรสิต/ปฐพี/ผู้สูงอายุ/ล่าม/…) → gap +23.3→**+2.4**, TH mean 72.2→93.1; 🐛 junk 2 รายการหลุดเข้า dict (empty value + คู่ที่ไม่ใช่ pair) → ตัดทิ้งทันที
- **Wave 2 (hybrid dense+lexical):** `dsa_utils.FastInvertedIndex` เปลี่ยนเป็น **Okapi BM25 จริง** (IDF `ln((N-n+0.5)/(n+0.5)+1)`, tf/(tf+k1(1-b+b·|d|/avgdl)), k1=1.2 b=0.75 — แทน IDF custom `1+100/(n+1)` + avgdl=50 hardcode); ใหม่ `ThreadSafeInvertedIndex` (frozen publish ใต้ lock, reader ไม่เจอ半成品) + `core/corpus_index.py` (tokenizer: ASCII whitespace + **Thai character bigrams** ไม่มี pythainlp dep ใหม่; corpus = `embedding_text` 5,685 docs build 0.42s ที่ startup lifespan) → lexical bonus ≤0.10 เข้า composite; ผล: gap **+1.9**, TH mean **96.5**, top1 20/20, spread กลับมา (99.0/97.8/95.6/92.9…)
- **Wave 3 (cyber + hygiene):**
  - OWASP header fix: `X-XSS-Protection: 0` (legacy auditor ถูกถอดจากเบราว์เซอร์แล้ว — เปิด = vector ใหม่), HSTS prod-only (localhost sticky-https guard; preload ไม่ใส่ — irreversible), `/docs` `/redoc` = None เมื่อ DEBUG=False
  - `RateLimiter`: **stale-IP key eviction** (amortized sweep ทุก 512 mutations — เดิม dict โตตามจำนวน IP ไม่มีวันลด = memory leak + DoS amplification)
  - **Tiered limiter (B-4):** POST AI-quota endpoints ได้ bucket เฉพาะ — cold-email 10/min, career-quiz/analyze 15/min, labs/inquiry 15/min, search 40/min (พิสูจน์: remaining 14→13→12 บน inquiry); longest-prefix-wins กัน `/search/cold-email` ตก bucket ผิด; B-2: `_client_ip` + trust-boundary comment (XFF ซ้ายสุด spoof ได้ — ยอมรับเฉพาะหลัง reverse proxy)
  - SQL injection ปิดเพิ่ม 2 จุด: `routes_labs.py` ILIKE filters → bound params (`:u_pat`/`:f_pat`) + N+1 (≤40 round-trips → 2 ด้วย `_resolve_labs_faculty_map` prefetch `id.in_()`); `fuzzy_dedup.py` example-SELECT + batch-DELETE → bind parameters (เลิก manual `''` escaping); `migrate_supabase_to_local.py` → `_safe_ident` regex allowlist กัน identifier จาก config (defensive — values hardcode อยู่แล้ว)
  - Dead code: frontend `ClientTrie` + phantom `FastRanker` doc ลบ (grep 0 refs), backend `Trie` import 2 จุดลบ; #7 precompile inline regex 3 จุด (routes_courses `_IT_WORD_RE`, quiz_schema `_NON_NUMERIC_RE`, security `_CONTROL_CHARS_RE`)
- **#4 ตัดสินแล้ว (วัดก่อนตัดสินใจ):** keyword_fallback OR 8 คอลัมน์ — Postgres BitmapOr เฉพาะเมื่อ **ทุก branch มี index**: สร้าง GIN trgm เพิ่ม 5 คัน (first/last_name, department, faculty, **embedding_text**) → selective query 91ms→**0.86ms** (สมศรี), high-hit (กฎหมาย, 80/5,685 rows) planner เลือก seq-scan ถูกต้องตาม cost model → index ไม่ทำให้แย่ลง; +5.8MB on disk; sync `docker/init.sql` ครบ
- **ปฏิเสธรายการวิจัยอย่างมีหลักฐาน:** hybrid agent Rec 6 `SET LOCAL hnsw.ef_search=80` — **ถอยหลัง** (วัด: ef≥64 flips 5.7k-row faculties → Seq Scan 22–35ms vs 2.4ms; และ SET LOCAL นอก transaction = no-op); **RRF fusion defer** (additive composite + calibrated bonuses นำอยู่ — benchmark ต้องไม่ถอยก่อน swap); **semantic-cache บน /search defer** (SearchResponse ขึ้นกับ university/faculty/department/top_k filters — serving cached payload = แอบ ignore filters ของ request ใหม่; embedding expensive ส่วนนั้น LRU-cached แล้ว); pythainlp defer (dep ใหม่ไม่คุ้ม bigrams)
- **⚠️ A-1 ต้องตัดสินใจโดย user (ไม่ได้แตะ):** `.env` เคย commit ใน `420123b` (repo สาธารณะ golfdesu/thai-advisor-match) — `DATABASE_URL` (Supabase password) ยังอ่านย้อนได้จาก `git log -p`; `4a33b5c` ลบแค่ปัจจุบัน ไม่ใช่ history; ทางออก = **rotate credential** (อย่างเดียวที่ปิดจริง) + พิจารณา `git filter-repo`/BFG (irreversible → user เท่านั้น)
- **Verification gate:** pytest 11/12 (fail เดียว = stale-slug documented เดิม; "hang" ก่อนหน้า = buffering ของ background pipe ไม่ใช่ test ค้าง), benchmark floors ผ่านทุกตัว, frontend tsc-clean (home/advisor 200 หลังลบ ClientTrie), backend live bfviv4fuc
