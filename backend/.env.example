# Environment Configuration
PROJECT_NAME="Thai Advisor Match API"
DEBUG=True
PORT=8000
HOST="0.0.0.0"

# AI / Embedding Providers (Optional: system has local TF-IDF / keyword fallback if key not provided)
GEMINI_API_KEY=""

# Vector & Database Configuration
DATABASE_URL="sqlite:///./data/advisor_match.db"
DATA_DIR="./data"
